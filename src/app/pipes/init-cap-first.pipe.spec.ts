import { InitCapFirstPipe } from './init-cap-first.pipe';

describe('InitCapFirstPipe', () => {
  it('create an instance', () => {
    const pipe = new InitCapFirstPipe();
    expect(pipe).toBeTruthy();
  });
});
