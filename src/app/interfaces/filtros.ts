export interface Filtros {
  fechaDesde: string;
  fechaHasta: string;
  numeroIncidencia: string;
  tipoIncidencia: string;
  origen: string;
  destino: string;
  ots: string;
  transporte: string;  
  estado: string;    
}